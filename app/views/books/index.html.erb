<div class="row">

<%= render '/users/sidebar', book: @book %>

<div class="col-lg-9">

<h1>投稿一覧</h1>
<% @books.each do |book| %>
  <%= link_to book_path(book.id) do %>
    <h4>title</h4>
  <% end %>
  <%= book.title %>
  <h4>opinion</h4>
  <%= book.body %>
<% end %>

</div>

</div>